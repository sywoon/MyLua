
local AL = class("AttachmentLoader")


function AL:ctor()
end

function AL:newRegionAttachment(skin, name, path)
    assert(false, "newRegionAttachment")
end

function AL:newMeshAttachment(skin, name, path)
    assert(false, "newMeshAttachment")
end

function AL:newBoundingBoxAttachment(skin, name)
    assert(false, "newBoundingBoxAttachment")
end

function AL:newPathAttachment(skin, name)
    assert(false, "newPathAttachment")
end

function AL:newPointAttachment(skin, name)
    assert(false, "newPointAttachment")
end

function AL:newClippingAttachment(skin, name)
    assert(false, "newClippingAttachment")
end



return AL